<section class="ui left aligned middle aligned segment">

    <h2 class="ui header">My Phone Categories</h2>

    <form action="/dashboard" method="GET" class="ui form" style="margin-bottom: 1em;">
        <div class="ui inline fields">
            <div class="field">
                <div class="ui icon input">
                    <input type="text" name="searchTerm" placeholder="Search by title..." value="{{search}}">
                    <i class="search icon"></i>
                </div>
            </div>
            <div class="field">
                <label>Sort by:</label>
                <select class="ui dropdown" name="sort" onchange="this.form.submit()">
                    <option value="">-- none --</option>
                    <option value="title" {{#if titleSelected}}selected{{/if}}>Title (A–Z)</option>
                    <option value="rating" {{#if ratingSelected}}selected{{/if}}>Rating</option>
                </select>
            </div>
            <div class="field">
                <label>Order:</label>
                <select class="ui dropdown" name="order" onchange="this.form.submit()">
                    <option value="asc"  {{#if ascSelected}}selected{{/if}}>↑ Low to High</option>
                    <option value="desc" {{#if descSelected}}selected{{/if}}>↓ High to Low</option>
                </select>
            </div>
            <div class="field">
                <button class="ui blue button" type="submit">Search</button>
                <a class="ui button" href="/dashboard">Clear</a>
            </div>
        </div>
    </form>

    {{#unless playlists}}
        <div class="ui info message">
            No categories yet — add one below!
        </div>
    {{/unless}}

    <div class="ui centered cards">
        {{#each playlists}}
            <div class="ui card">
                {{#if picture}}
                    <div class="image">
                        <img src="{{picture.url}}" alt="{{title}}">
                    </div>
                {{/if}}
                <div class="content">
                    <a class="header">{{title}}</a>
                    <p>
                        <i class="mobile alternate icon"></i>
                        {{#if phones}}
                            Total phones: {{phones.length}}
                        {{else}}
                            Empty category!
                        {{/if}}
                    </p>
                    {{#if rating}}
                        <p>
              <span
                      class="ui yellow disabled rating"
                      data-icon="star"
                      data-rating="{{rating}}"
                      data-max-rating="5">
              </span>
                        </p>
                    {{/if}}
                </div>
                <div class="extra content">
                    <a href="/playlist/{{id}}">
                        <i class="ui open folder outline black icon"></i>
                    </a>
                    <a href="/dashboard/deleteplaylist/{{id}}"
                       onclick="return confirm('Delete this category?')">
                        <i class="ui black trash icon"></i>
                    </a>
                </div>
            </div>
        {{/each}}
    </div>

</section>